# Netlify redirects file
# Remove trailing slashes and handle SPA routing

# Remove trailing slashes (301 permanent redirect)
/*/ /:splat 301!

# SPA fallback - all routes go to index.html
/* /index.html 200
